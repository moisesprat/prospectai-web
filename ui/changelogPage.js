/* ============================================================
   CHANGELOG PAGE — static content, nav wiring only.
   ============================================================ */

import { initNav } from './nav.js';
initNav();
